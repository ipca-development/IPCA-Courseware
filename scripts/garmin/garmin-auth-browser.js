#!/usr/bin/env node
console.error('garmin-auth-browser.js is retired. The persistent garmin-worker owns the only Chromium profile; use garmin-auth-session.sh to expose that browser through VNC.');
process.exit(1);
