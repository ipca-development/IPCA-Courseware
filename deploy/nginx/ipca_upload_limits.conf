# Large EASA XML uploads (52MB+). Include inside your server { } or http { } block, then:
#   nginx -t && systemctl reload nginx
#
# Default nginx client_max_body_size is 1m; many templates use ~25m — uploads then stall near that size
# even when PHP reports upload_max_filesize=128M.

client_max_body_size 128m;
